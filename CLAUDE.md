# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Emoji Policy

**NO emoji characters anywhere in this project** — not in templates, not in Python files, not in HTML, not in content. Never add emoji unless explicitly instructed by Kahu Phil.

## Project Overview

Flask web application for Ke Aupuni O Ke Akua, a Hawaiian Kingdom ministry website. Deployed on Render.com at `keaupuniakeakua.faith`.

## Commands

- **Run locally:** `flask run` or `python app.py`
- **Install dependencies:** `pip install -r requirements.txt`
- **Production start:** `gunicorn --bind 0.0.0.0:$PORT app:app`
- **Run tests:** `python -m pytest` (361 tests as of 2026-08-03; this line was stale — a real suite has existed since at least Work Order P-001). No linter configured.

## Architecture

**Blueprint-based Flask app** refactored from a legacy monolith (`ke_aupuni_website.py`, still present but unused).

### Entry Points
- `app.py` — App factory, registers all blueprints
- `config.py` — Centralized settings (page nav order, paths). All secrets loaded from environment variables (no hardcoded fallbacks).
- `content.py` — JSON data layer: reads/writes `website_content.json` and `digital_products.json`

### Blueprints (`blueprints/`)
| Blueprint | Route prefix | Purpose |
|-----------|-------------|---------|
| `pages.py` | `/<page_id>` | Public content pages |
| `downloads.py` | `/download/` | Free PDF downloads |
| `products.py` | `/product/`, `/admin/products` | Digital product CRUD + public pages |
| `payments.py` | `/checkout/`, `/paypal/`, `/stripe/` | PayPal (active) + Stripe (ready to activate) |
| `admin.py` | `/kahu` | Admin panel (password: see `config.py`) |
| `publishing_media.py` | `/publishing-media/` | Signed-URL bridge serving approved Discovery Workforce campaign media (Shorts, images) to Buffer/YouTube/Facebook |

### Data Storage
Content is stored as JSON files, **not a database**:
- `website_content.json` — Page content (Markdown rendered to HTML via `markdown` library with `extra` and `nl2br` extensions). **Do not delete.**
- `digital_products.json` — Product catalog. **Do not delete.**
- `digital_products/` — Uploaded product files

### Templates
Jinja2 templates in `templates/` extend `base.html`. CSS is inline via `{% include 'partials/styles.css' %}`. No CSS framework is used — all custom responsive styles.

### Deployment
- Primary: Render.com (`render.yaml`)
- `Procfile` for Heroku-compatible hosts
- `vercel.json` exists but is outdated (references old monolith)

### Page Navigation Order
Defined in `config.py` as `ORDER` list. Controls nav bar ordering.

### Sales Funnels
- `templates/aloha_wellness_funnel.html` — Standalone landing page for Aloha Wellness book. Routes to `/checkout/prod_aloha_wellness` (uses existing PayPal/Stripe payment system). Includes free booklet email capture via `/download/aloha_wellness_freebie`.

## Environment Variables (Required on Render)

All secrets are loaded via `os.environ[]` with **no fallback defaults** — the app will crash on startup if any are missing. Set these in Render Dashboard > Environment:

| Variable | Description |
|----------|-------------|
| `ADMIN_PASSWORD` | Password for `/kahu` admin panel |
| `PAYPAL_CLIENT_ID` | PayPal live API client ID |
| `PAYPAL_CLIENT_SECRET` | PayPal live API secret |
| `SMTP_HOST` | SMTP server hostname (e.g. `mail.privateemail.com`) |
| `SMTP_PORT` | SMTP port (e.g. `587`) |
| `SMTP_USER` | SMTP login email address |
| `SMTP_PASS` | SMTP login password |
| `FLASK_SECRET_KEY` | Signs the admin (`/kahu`) session cookie. If unset, the app still runs (a random key is generated at process start) but every admin session is invalidated on every restart/redeploy. Added 2026-07-17; was missing from this table until 2026-07-29. |
| `DOWNLOAD_TOKEN_SECRET` | Signs the time-limited, product-bound tokens minted after a verified PayPal/Stripe payment (`download_tokens.py`) and required by `/download/product/<id>/<token>`. If unset, digital-product downloads fail closed (503) for every product — this is a single, application-wide secret; it does not need to be set per product. Added 2026-07-23; was missing from this table until 2026-07-29, which is the confirmed root cause of a production incident where automatic post-purchase downloads were unavailable. Reused (same value, different `itsdangerous` salt) by `publishing_media_tokens.py` for the Discovery Workforce media bridge (`/publishing-media/<asset_id>/<token>`) — added 2026-08-03; deliberately not a second secret. |

**Optional** (safe empty-string defaults):
- `STRIPE_PUBLISHABLE_KEY`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET` — Set when activating Stripe
- `STRIPE_ENABLED` — Set to `true` to show Stripe button on checkout

## Change Log

### 2026-08-05 (Work Order V001-003 Revision — Hero Nav-Overlap and Contrast Fix)
- **Two real UI defects found during human visual review of the V001-002 front-door rebuild**, both in the shared `.hero`/`.hero-overlay` classes (`templates/partials/styles.css`) used by every hero-image page (home, `/rotten-fencepost`, `/campaign/<id>`) -- not unique to one template.
- **Defect 1 (nav overlap, high priority)**: `.hero-content` is bottom-anchored (`align-items: flex-end`), so its clearance from the fixed `.site-nav` depends entirely on the hero box being tall enough -- padding/margin on the content itself can't create that clearance, since a bottom-anchored block's position is set by (box height − content height), independent of top padding. `.hero`'s `min-height` floor of 600px wasn't enough once you add the nav's worst-case height (~230px, driven by the 210px logo) plus a wrapped title -- reproducible on realistic short browser windows (real toolbars/bookmarks bars routinely leave well under 700px of usable height on common 1366×768/1440×900 laptop screens, even though 100vh/80vh alone suggests more room). Not reproducible in a maximized headless browser, which has no chrome to subtract -- confirmed by deliberately testing a short viewport (1366×600) locally. **Fix**: raised `min-height` to 700px.
- **Defect 2 (contrast)**: `.hero-overlay`'s gradient darkened only the bottom of the photo (0.7 opacity, for the caption) and left the top -- where the nav floats -- almost untouched (0.1 opacity), so bright sky/background could wash out the nav text. **Fix**: gradient now darkens both top and bottom (0.55 / 0.75), lighter through the middle -- same photo, no artwork replaced, better contrast at both the nav and the caption.
- **Scope discipline**: both fixes are in the one shared CSS class pair, not per-template -- no route, template, or content changed; no redesign. Verified the homepage and `/campaign/001` heroes (same shared classes) and the product page (different template, confirmed unaffected) all still render correctly after the change.
- **Testing**: full suite **380/380 pass** (unchanged -- this is a pure CSS fix, no test additions needed). Full local sitemap crawl (41 URLs): zero broken links. Verified via headless Chrome screenshots at multiple viewport sizes (900px–1600px height, several widths spanning the hamburger-breakpoint boundary), including the short-viewport case that reproduces the original defect, confirming the fix holds there while normal-height desktop views are visually unchanged.
- **Approved by Kahu Phil.** Committed, pushed, and deployed to Render. **Live-verified via HTTP against `keaupuniakeakua.faith`**: see below.

### 2026-08-05 (Work Order V001-002 — Rotten Fencepost Front Door: Principle-First Restructure)
- **Brand architecture change, approved by Kahu Phil via a wireframe review before any code was touched**: `/rotten-fencepost` is restructured around the governing definition that Rotten Fencepost is a *principle* (recurring problems are solved by finding causes, not repeatedly treating symptoms) and every book/video/field guide is a doorway into it, not the other way around. Five approved sections, in order: (1) The Rotten Fencepost Principle, (2) Begin Your Journey (Find the Cause, Not the Symptoms, $9.99 — the primary introduction), (3) Watch the Foundation Series (unchanged video hub embed), (4) Study the Principle (Rotten Fencepost Field Guide, $9 — the applied companion), (5) A Personal Invitation from Kahu Phil (closing). Same URL, same two products, same video — content reordered and retitled, none of it deleted.
- **`templates/rotten_fencepost.html`**: full reorder. `<h1>` and `<title>` changed from "The Rotten Fencepost Field Guide" to "The Rotten Fencepost Principle" — the one real SEO-relevant change, deliberately aligning the hub with the phrase already used consistently by the wellness article, the evergreen article, and the video's own subtitle. Canonical tag, URL, and sitemap entry are unchanged. Added one new internal link to the evergreen article (`/rotten-fencepost/find-the-cause-not-the-symptoms`), previously unlinked from this page. No placeholder sections added for a workbook or future resources — per explicit instruction, the page is built only from what exists in the catalog today.
- **`blueprints/pages.py`**: added a `page_schema` (`WebPage`, name "The Rotten Fencepost Principle") to `rotten_fencepost()`, alongside the pre-existing `product_schema` (Field Guide, $9, unchanged — still the only canonical sales surface for that catalog entry, so its schema stays here rather than moving to the Find the Cause card).
- **Testing**: new `tests/test_rotten_fencepost_front_door.py` (17 tests) — Principle-led H1/title/WebPage schema, the five sections present in the approved order (verified by string position in the rendered HTML, not just presence), Begin Your Journey features Find the Cause before any checkout link appears, Field Guide checkout link preserved, both companion-article links present, no "Workbook" or generic future-resources text anywhere, Product schema and canonical tag unchanged. Updated one pre-existing test in `tests/test_campaign_pages.py` that hardcoded the old product-led H1. Full suite: **380/380 pass** (362 prior + 17 new + 1 updated). Full sitemap crawl (41 URLs) and a crawl of every link rendered on `/rotten-fencepost` itself: zero broken links, zero non-200s. Verified section order against a real local `flask run` instance, not just template source. Claude-in-Chrome was unavailable this session (same recurring limitation as prior UI work orders) — pixel-level visual review (spacing, hero crop, mobile layout) was not possible and is flagged for a human look at/after deploy.
- **Approved by Kahu Phil (wireframe reviewed and approved before implementation; brand architecture and implementation both explicitly authorized).** Committed (`133845c`), pushed, and deployed to Render. **Live-verified via HTTP against `keaupuniakeakua.faith`**: `/rotten-fencepost` returns 200; `<h1>` reads "The Rotten Fencepost Principle"; `<title>` and canonical tag correct; the five approved sections appear in order (verified by string position in the live HTML); JSON-LD carries `Organization`/`Person`/`WebSite`/`WebPage`/`Product`, with the new `WebPage` entity named "The Rotten Fencepost Principle" and the pre-existing `Product` entity unchanged at $9.00; every link (Find the Cause, Field Guide checkout, the campaign video page, both companion articles) present and correct; no "Workbook" text anywhere. Full live sitemap crawl (41 URLs, zero duplicates): zero non-200s, no regressions elsewhere on the site.

### 2026-08-03 (Campaign 001 — Evergreen Article + Discovery Workforce Publishing-Media Bridge)

**Status: tested locally, NOT committed, pushed, or deployed — awaiting Kahu Phil's explicit authorization.**

- **New evergreen article page** at `/rotten-fencepost/find-the-cause-not-the-symptoms` (`blueprints/pages.py`'s existing `_SEO_PAGES`/generic `/<parent>/<slug>` route — no new route or template), body sourced verbatim from the Discovery Engine's real, finished `ARTICLE_01_WEBSITE_READY.md` (710 words), title/meta from `ARTICLE_01_METADATA.json`. Placed under `rotten-fencepost` (general-audience: health, finances, relationships, leadership), not `wellness` (diet/health-specific), per Kahu Phil's explicit decision — distinct from the existing `/wellness/the-rotten-fencepost-principle` article, which it links to as a companion piece rather than duplicating. `hero_image` uses the already-live `static/covers/find_the_cause_not_the_symptoms_cover_web.jpg` (confirmed reachable via a direct HTTPS GET before use — not a guessed path). Added one `related_links` entry to `content.py`'s `CAMPAIGNS["001"]` and one line to the manually-curated `sitemap()` list in `blueprints/pages.py`, matching the existing pattern exactly.
- **New Discovery Workforce publishing-media bridge** — `/publishing-media/<asset_id>/<token>` (`blueprints/publishing_media.py`), gated by a signed, asset-bound, time-limited token (`publishing_media_tokens.py`, 14-day expiry) reusing the existing `DOWNLOAD_TOKEN_SECRET` with a distinct `itsdangerous` salt (`"publishing-media"`) — no second Render secret needed. Same fail-closed convention as `/download/product/<id>/<token>`: missing secret → 503, anything else wrong (expired/tampered/wrong-asset/unregistered) → 403/404, never distinguished further. The route only ever resolves an `asset_id` through a server-side manifest (`publishing_media/manifest.json`) to a real filename inside `publishing_media/` — it never accepts a raw filename/path from the request, and re-verifies the resolved path stays inside that directory before serving (defense in depth). No directory-listing route exists.
- **`publish_media.py`** (repo-root CLI + importable functions) is how the Discovery Engine's Publishing Department (a separate local codebase) moves an already-approved asset in — `copy_asset_into_media_dir()` (pure file/manifest bookkeeping, needs no secret) and `build_public_url()` (mints the actual signed URL, needs the real `DOWNLOAD_TOKEN_SECRET` — can be (re)run at any later time, since the token is a pure function of secret + asset id, not stored state). `register()` combines both for the CLI. `mark-verified`/`cleanup` implement the retention rule: nothing is ever deleted before Publication Verification independently confirms an asset is genuinely live, and even then only after a 7-day grace period.
- **No persistent disk on this host** — `publishing_media/` is committed to git exactly like the existing `digital_products/` (this app's only "deploy artifact" is the repo itself). The 5 real, finished Campaign 001 Shorts (edited from the actual published video `GywmvlrxXQ0`'s official captions/audio — not fabricated; see the Discovery Engine's own `PROJECT_LOG.md`, 2026-08-03) are already staged into `publishing_media/` via `copy_asset_into_media_dir()`, ready to commit alongside this code.
- **Testing**: `tests/test_publishing_media.py` (15 tests: register/copy idempotency and re-copy-on-change, URL validity, tampered/wrong-asset/never-registered/missing-secret rejection, path-traversal refusal even with a corrupted manifest entry, cleanup's verified-only + grace-period rule, the copy/URL-mint split, and the real `campaign_001:planning_document:short_01:v2`-shaped asset id — which contains `:`, invalid in a Windows filename, caught by this test before it could have broken in production) and `tests/test_rotten_fencepost_article.py` (11 tests: 200/title/meta/canonical/Article schema, real hero image, links to the hub/product/companion article, no internal `campaign_001` identifier leaking onto the public page, the campaign page's related links include it, sitemap includes it exactly once with zero sitewide duplicates). **Full suite: 361/361 pass** (335 prior + 15 + 11 = 361, confirmed via `pytest --collect-only`).
- **Root-cause fix, found via testing, not yet related to Campaign 001 itself**: `production_config.WEBSITE_REPO_DIR` in the Discovery Engine (a different codebase, not this repo) pointed at `C:\Users\MangoMain\Documents\My-Website`, which does not exist on this machine (`Documents` is OneDrive-redirected) — corrected there to `C:\Users\MangoMain\OneDrive\Documents\My-Website`. With the corrected path, the Discovery Engine's own website-commit-preparation step now genuinely runs this repo's real test suite (previously it silently failed against the wrong path, misreported as "10 errors" — this repo's code was never actually at fault).
- **Not yet done, by design — genuine owner actions remain:**
  1. Review and authorize the commit below (nothing has been committed).
  2. Once authorized: commit, push, and deploy per this file's own deploy discipline, then live-verify per past convention.
  3. After deploy, someone with the real, deployed `DOWNLOAD_TOKEN_SECRET` runs `python publish_media.py build-url campaign_001:planning_document:short_0X:v2` for each of the 5 Shorts (from a local shell with that secret exported, or Render's own console) to mint the actual usable public URLs — this cannot be done from the Discovery Engine side without duplicating a production secret into a second codebase, which was deliberately not done.
- **Exact commit contents (not yet staged):** modified `app.py`, `blueprints/pages.py`, `config.py`, `content.py`, `CLAUDE.md`; new `blueprints/publishing_media.py`, `publish_media.py`, `publishing_media_tokens.py`, `tests/test_publishing_media.py`, `tests/test_rotten_fencepost_article.py`, `publishing_media/` (5 `.mp4` files + `manifest.json`). Two files modified by an unrelated, already-in-progress prior session (`DISCOVERY_OPERATIONS_EXECUTION_LOG.md`, `discovery_distribution_tracker.csv`) are deliberately excluded from this commit.

### 2026-07-31 (Work Order D-003 — Goodreads Author Identity Alignment)
- **Audit finding:** the site had no page a Goodreads reviewer (or any external verifier) could land on and confirm all three required identity facts — official name, official website, official contact — within about a minute. The name "Kahu Phil Stephens" was already consistent everywhere it appears (confirmed by a sitewide grep across `templates/`, `content.py`, `blueprints/`). `config.py`'s `CONTACT_EMAIL` (`kahuphil@keaupuni.faith`) already matched the work order's required contact exactly, but it was rendered on exactly one page sitewide: `payment_success.html`, an error-path, `noindex` transactional page reached only after a payment succeeds but its automatic download fails — effectively invisible to a normal visitor and certainly not reachable from the homepage in any reasonable time. No About/Author page existed at all.
- **Fix — new `/author` page** (`content.py`, using the existing `DEFAULT_PAGES`/generic `page.html`/`/<page_id>` route pattern — no new template, route, or nav entry). States the three required facts as plain visible text: name, "Official website: keaupuniakeakua.faith" (this site), and the contact email as a `mailto:` link. Bio paragraph reused verbatim from the existing "About Kahu Phil Stephens" text already published on `myron_golden_funnel.html` — no new biographical claims. Links only to catalog entries confirmed `active: true` with real files in `digital_products.json` (`prod_find_the_cause_not_the_symptoms`, `prod_aloha_wellness`) plus `/products` — no unpublished or fabricated titles listed. Added to `sitemap.xml` (`blueprints/pages.py`) at low priority/monthly, same as every other real page.
- **Reinforcement, not redesign:** added one link ("Kahu Phil Stephens" → `/author`) inside the homepage's existing "Aloha. You are in the right place." bio sentence (`templates/page.html`) — the natural point a visitor arriving at the official website (the homepage) already reads the author's name — rather than adding anything to the sitewide nav or footer. Added `"email": "kahuphil@keaupuni.faith"` to the sitewide Person JSON-LD entity in `templates/base.html` (one line, additive) so the same contact fact is also machine-readable on every page, matching `config.py`'s existing `CONTACT_EMAIL` value. Person `name`/`url`/`jobTitle`/`description` — all already correct — were left untouched.
- **No architecture, nav, or Product 001 changes**: `get_nav_items()`/`config.py`'s `ORDER` (confirmed unused by `base.html`, which hardcodes its nav markup) were not touched; the new page isn't in either. `digital_products.json`, `product_page.html`, and every existing product/checkout route are unchanged — Product 001 itself was not modified, only linked to from the new page (same as `/products` already does).
- **Testing**: new `tests/test_author_identity.py` (15 tests) — `/author` returns 200 and states all three identity facts (name, website, `mailto:` contact); its `WebPage` schema and canonical tag are correct; every `/product/<id>` link on the page resolves 200 (guards against ever citing an unpublished/fabricated title); homepage links to `/author`; sitewide Person schema carries the new `email` alongside its unchanged `name`/`url`; sitemap includes `/author` exactly once; and a regression class confirms Product 001's page, `/products`, and `/rotten-fencepost` are unaffected. Full suite: **335/335 pass** (320 prior + 15 new). Full sitemap crawl (40 URLs, was 39): zero duplicates, zero non-200s.
- **Goodreads readiness**: the website side of author verification is now addressable — a reviewer following the official-website link from the Goodreads Author Program submission can reach `/author` (directly, or one click from the homepage) and confirm name, website, and contact within well under a minute. This does not itself submit, approve, or claim anything on Goodreads — per the tracker (`discovery_distribution_tracker.csv`), the book-catalog entry is still **Pending External Approval** (blocked on Kahu Phil's Goodreads account, outside this codebase's control) and the Author Program claim (step 2 of 2) still depends on that clearing first.
- **Approved by Kahu Phil, committed (`81b4548`), pushed, and deployed to Render.** **Live-verified via HTTP against `keaupuniakeakua.faith`**: `/author` returns 200 and its rendered HTML states the official name, the official-website text, and the contact email as a working `mailto:` link; its canonical tag and the link to Product 001 are both correct. `/sitemap.xml` lists `/author` exactly once. The homepage's rendered HTML links to `/author`, and the sitewide Person JSON-LD now includes `"email": "kahuphil@keaupuni.faith"` alongside its unchanged `name`/`url`. `/products`, `/product/prod_find_the_cause_not_the_symptoms`, and `/rotten-fencepost` all reconfirmed 200 — no regressions from this change reached production.

### 2026-07-29 (Work Order A-003 — Download Secret Root-Cause Fix)
- **Root cause identified and fixed for the production incident where automatic post-purchase downloads returned 503 for every digital product.** A read-only audit traced it to `DOWNLOAD_TOKEN_SECRET` (added 2026-07-23, commit `ffb26c0`) never having been set on Render — unlike every other secret this project has added, it never received a corresponding "set this in Render" note in this Change Log or in the "Environment Variables (Required on Render)" table above, so nothing ever prompted anyone to configure it. Full audit trail: this session's prior root-cause report.
- **Fix applied:** added the two rows above for `FLASK_SECRET_KEY` and `DOWNLOAD_TOKEN_SECRET` (both previously missing from this table) so the documented checklist matches what the code actually requires. This is the only code/doc change in this work order — the secret value itself was generated locally and given to Kahu Phil directly to enter in Render's dashboard; it was never written to any file, log, or commit in this repository.
- **Also confirmed, and worth recording:** `DOWNLOAD_TOKEN_SECRET` is a single, application-wide secret (not per-product) — every current and future digital product's download route reads the same `config.py` value, so no further Render configuration is needed as the catalog grows.
- **Testing**: full suite re-run, no functional code touched (documentation-only change).

### 2026-07-29 (Work Order A-002 — Publisher Identity Migration)
- **Established "Rotten Fencepost Publishing" as the ecosystem's publishing imprint**, distinct from "Ke Aupuni O Ke Akua" (the ministry/organization identity, unchanged). Follows Work Order A-001 (Publishing Identity Audit), which found three inconsistent name variants ("Ke Aupuni O Ke Akua", "...Press", "...Publishing") live simultaneously across the site.
- **Interpreted per-occurrence, not a blind find-and-replace** — only changed locations unambiguously functioning as a book/product publisher or copyright holder: `digital_products.json`'s `publisher` field (Product 001), `/products` page copy and OG description, the Aloha Wellness ebook's own title-page byline and copyright page, `kingdom_study.html` and `aloha_wellness_funnel.html` footer copyrights (both are individual paid-product pages), the Discovery Operations documents that quote the publisher field for external submissions (Goodreads, etc.), and `schema.py`'s `build_product_schema` `brand` field (now sourced from a product's own `publisher` when present, falling back to the Organization entity otherwise — keeps Product 001's JSON-LD `brand` consistent with its catalog data without touching any other product).
- **Deliberately left unchanged, flagged for a future decision** (all still read "Ke Aupuni O Ke Akua Press" or bare "Ke Aupuni O Ke Akua"):
  - `templates/base.html`'s sitewide Schema.org `Organization` entity (`#organization`) — it's reused by `schema.py` as the `publisher`/`brand` for every Article, WebPage, and non-Rotten-Fencepost Product; renaming it either direction is an architecture decision (whether ministry and publisher should be separate schema.org entities), not a string swap.
  - `templates/checkout.html`'s purchase-header subtitle and `templates/product_page.html`'s publisher fallback — both render for every product including ministry-partnership donation tiers and the Nahenahe Voice CD, where "Rotten Fencepost Publishing" would misrepresent the purchase.
  - `templates/myron_golden.html` / `myron_golden_funnel.html` footers (affiliate marketing pages for a third party's training, not Kahu Phil's own published work).
  - `data-deletion.html` (Meta/Facebook data-deletion compliance page — unrelated to book publishing) and `blueprints/downloads.py`'s newsletter welcome email (sitewide list-signup confirmation, not a book receipt) — both still say "...Press", which is neither the ministry's nor the publisher's correct name; left untouched as out of this work order's scope, noted for a possible separate ministry-name cleanup pass.
  - `GOOGLE_VISIBILITY_READINESS_AUDIT.md` — a dated historical audit snapshot; not edited to preserve the historical record.
- **`DISCOVERY_OPERATIONS_EXECUTION_LOG.md`**: added a dated addendum directly under the 2026-07-29 "Business name confirmed" entry reconciling the two decisions — that entry settled the *business-listing* name (Bing Places/GBP = "Ke Aupuni O Ke Akua", unaffected by this work order); this work order separately settles the *publisher-of-record* name for books/products ("Rotten Fencepost Publishing"). The original entry's text was preserved verbatim, not edited, to keep the historical record intact.
- **Testing**: no new tests added (a content/copy migration, not new behavior) — full existing suite re-run: **320/320 pass**. Manually verified via the Flask test client that `/products`, `/product/prod_find_the_cause_not_the_symptoms`, `/kingdom-study`, and `/aloha-wellness` render "Rotten Fencepost Publishing" and that `/product/prod_aloha_wellness` (no `publisher` field) correctly still falls back to the unchanged "Ke Aupuni O Ke Akua Press" text pending the flagged decision above.
- **Not yet committed, pushed, or deployed** — awaiting Kahu Phil's explicit go-ahead, per standing deploy discipline.

### 2026-07-28 (Work Order P-003)
- **Scalable hub/campaign video architecture** (Discovery Operations Work Order P-003, revised before implementation to plan for 60+ future campaigns and their Shorts/assets). The dedicated `/campaign/001` page, its route, and the reusable `CAMPAIGNS`/generic-campaign architecture from Work Order P-001 are unchanged in shape — this work order adds a curated hub embed and forward-looking (currently empty) data fields, not new routes or a template redesign.
  - **`content.py`**: added `HUB_FEATURED_CAMPAIGN_IDS` — an explicitly curated list (currently `["001"]`) controlling which campaigns are surfaced as playable videos on `/rotten-fencepost`, and in what order. This is deliberate: with 60+ campaigns eventually expected, the hub must never loop over every `CAMPAIGNS` entry, or the page grows unbounded — a maintainer adds a campaign's id here only when it should be featured on the hub; every campaign is always reachable at its own `/campaign/<id>` regardless. New `get_hub_featured_campaigns()` resolves that list to campaign dicts. Also added `"id": "001"` to the `CAMPAIGNS["001"]` entry itself (so hub cards can build their own `/campaign/<id>` link generically) and a new `"shorts": []` field, documented as ready for real Shorts data (`{"youtube_id": ..., "title": ...}` per entry) but deliberately left empty — no placeholder/fabricated Shorts were added, per instruction.
  - **`blueprints/pages.py`**: `rotten_fencepost()` now calls `get_hub_featured_campaigns()` (instead of a hardcoded single-campaign lookup) and passes the result as `hub_campaigns`. `campaign_page()` is unchanged.
  - **`templates/rotten_fencepost.html`**: replaced the old text-only "Prefer to watch it first?" link with a "Watch the Series" section that loops over `hub_campaigns` (today, one card). Each card renders the campaign's subtitle/title, a responsive embedded player (same 16:9 pattern `campaign_page.html` already uses), and a link to that campaign's full `/campaign/<id>` page ("See everything from this video — shorts, articles, and more →"). Because the loop is over the curated `hub_campaigns` list rather than all campaigns, this section stays bounded by design as the library grows — new campaigns require adding to `HUB_FEATURED_CAMPAIGN_IDS`, not template changes.
  - **`templates/campaign_page.html`**: added a "Shorts From This Video" section, guarded by `{% if campaign.shorts %}`, rendered between the main video and the existing "Go Deeper" section — a responsive grid of vertical (9:16) embeds, one per `campaign.shorts` entry. Renders nothing for Campaign 001 today (`shorts: []`), confirmed by test. This makes the dedicated page ready to hold the full asset set the work order describes (long-form video ✓ already live, Shorts ✓ structurally ready, articles/product/downloads ✓ already covered by the existing `related_links` list) without any further route or template redesign when real Shorts are added.
  - **No competing titles/descriptions, no "Campaign 001" exposed**: hub cards render `hc.title`/`hc.subtitle` from the same `CAMPAIGNS` entries `/campaign/001` reads from, so copy can't drift between the two surfaces. The hub's own H1 ("The Rotten Fencepost Field Guide") is untouched. Confirmed by test and manual grep that neither page renders "Campaign 001" (case-insensitive) anywhere.
  - **Testing**: `tests/test_campaign_pages.py` — reused and extended the existing `TestRottenFencepostHubEmbed` class (added a test asserting the hub renders exactly `len(HUB_FEATURED_CAMPAIGN_IDS)` embeds, not one per `CAMPAIGNS` entry), plus two new classes: `TestNoInternalIdentifierPublic` (no "campaign 001" text on either page) and `TestCampaignPageShorts` (no Shorts section renders when the list is empty; renders correctly, with the right video id and title, when temporarily populated in a test). Full suite: **320/320 pass** (315 prior + 5 new). Full local crawl of all 39 sitemap URLs: zero regressions, zero non-200s.
  - **Not yet committed, pushed, or deployed** — awaiting Kahu Phil's explicit go-ahead, per standing deploy discipline.

### 2026-07-28 (Work Order P-002)
- **Public UX Refinement of `/campaign/001`** (Discovery Operations Work Order P-002). Refinement only — no route, architecture, or schema-type changes. Work Order P-001's generic `/campaign/<id>` pattern, the `CAMPAIGNS` dict, and `campaign_page.html` all remain exactly as built.
  - **Removed the internal "Campaign 001" identifier from every visitor-facing surface.** `content.py`'s `CAMPAIGNS["001"]["title"]` changed from `"Campaign 001: Find the Cause, Not the Symptoms"` to `"Find the Cause, Not the Symptoms"` — since the H1 (`templates/campaign_page.html`), the browser `<title>` fallback, the `WebPage` schema `name`, and the 3-level `BreadcrumbList`'s third item (`blueprints/pages.py`'s `campaign_page()`) all already derived from this one field, fixing it at the source corrected all four without touching route or schema-builder code. `seo_title` (`"Watch: Find the Cause, Not the Symptoms | Rotten Fencepost"`) already had no internal terminology in it, so the actual `<title>` tag and `og:title`/`twitter:title` (which read `seo_title` via `self.title()`) needed no change. The `/campaign/001` URL itself is unchanged, per instruction (implementation detail, not public-facing).
  - **Added a subtitle.** New `CAMPAIGNS["001"]["subtitle"]` field: `"The Rotten Fencepost Principle"`. `templates/campaign_page.html`'s existing gold eyebrow line above the H1 (previously hardcoded `"Rotten Fencepost — Discovery Series"`) now renders `{{ campaign.subtitle }}` instead — same visual treatment, campaign-driven text instead of a hardcoded string, consistent with how every other campaign field already works.
  - **Promoted Rotten Fencepost into primary navigation** (`templates/base.html`): moved `<li><a href="/rotten-fencepost">Rotten Fencepost</a></li>` out of the "More" dropdown (`.dropdown-menu`, added in Work Order 002) and into the top-level `.nav-menu`, immediately after Home. No new markup, CSS, or JS — same `<li><a>` element, same list, different position. Confirmed only one nav block exists sitewide (`base.html`); the standalone templates that don't extend it (`aloha_wellness_funnel.html`, `kingdom_study.html`, `myron_golden_funnel.html`, checkout/success pages) never carried the primary nav and are unaffected. Desktop/mobile share the same `<ul id="navMenu">` markup (CSS media query only changes presentation), so no separate mobile fix was needed.
  - **Verification**: grepped the full rendered `/campaign/001` HTML for "campaign 001" (case-insensitive) — zero matches. Confirmed programmatically: `<title>` unchanged and correct, H1 reads "Find the Cause, Not the Symptoms", subtitle reads "The Rotten Fencepost Principle", `BreadcrumbList` JSON-LD's third item name reads "Find the Cause, Not the Symptoms", video iframe (`youtube.com/embed/GywmvlrxXQ0`) still renders, sitemap still lists `/campaign/001` with zero duplicates and every sitemap URL still resolves 200, "Rotten Fencepost" appears in the nav exactly once (no duplicate entry), positioned directly after Home. `/rotten-fencepost` hub's existing links (to `/campaign/001` and to Product 001) confirmed intact. No changes to `schema.py`, route definitions, the `CAMPAIGNS` data structure's shape, or any template beyond the two touched.
  - **Testing**: no new tests added — this is a data/copy/markup-order change fully covered by existing assertions. Full suite: **311/311 pass**, unchanged from Work Order P-001. `tests/test_campaign_pages.py`'s title/breadcrumb/OG assertions (which check `seo_title`, not the changed `title` field) all still pass unmodified.
  - **Not yet committed, pushed, or deployed** — awaiting Kahu Phil's explicit go-ahead, per standing deploy discipline.

### 2026-07-28
- **Campaign 001 Website Integration** (Discovery Operations Work Order P-001). Integrated the published Campaign 001 YouTube video (`https://www.youtube.com/watch?v=GywmvlrxXQ0`) into the site as a permanent evergreen discovery asset, via a new generic, reusable pattern rather than a one-off page.
  - **`content.py`**: added a `CAMPAIGNS` dict (currently one entry, `"001"`) and `get_campaign_by_id()`, following the same "data lives in code" convention already used for `DEFAULT_PAGES`. Each entry holds `title`/`seo_title`/`meta_description`/`hero_image`/`youtube_id`/`intro_html`/`related_links` — Campaign 002, 003, etc. require only a new dict entry.
  - **`blueprints/pages.py`**: new generic route `/campaign/<campaign_id>` (`campaign_page()`) — 404s on an unknown id, otherwise builds `WebPage` schema (via the existing `build_webpage_schema`, same builder `/partner` already uses) and a 3-level `BreadcrumbList` (Home -> Rotten Fencepost -> the campaign), matching the pattern `products.py` already established for product pages. No new schema types were added to `schema.py`. Also added `/campaign/001` as one line in the `sitemap()` route's existing hand-curated list (documented in that route's docstring: future campaigns need one more line here, same manual-curation precedent already used for every other entry, including the deliberate product exclusions).
  - **New `templates/campaign_page.html`**: extends `base.html` (inherits canonical tag, OG/Twitter defaults, sitewide Organization/Person/WebSite JSON-LD automatically, same as every other public template). Reuses the exact hero/dark-card/gold-accent visual language already established in `templates/rotten_fencepost.html` (same `.hero`/`.hero-overlay`/`.hero-content` classes, same `#1a1a1a`/`#c9a842` color scheme) — no new CSS added anywhere. Responsive 16:9 YouTube embed via a padding-bottom wrapper (inline styles only, consistent with this template family's existing all-inline-style convention). Links to `/rotten-fencepost` and to Product 001 (`/product/prod_find_the_cause_not_the_symptoms`) live in a "Go Deeper" section driven by the campaign's `related_links` list, plus a third link to `/wellness/the-rotten-fencepost-principle` (an existing, already-published article on the same idea).
  - **`templates/rotten_fencepost.html`**: added exactly one new sentence + link ("Prefer to watch it first? Watch the Rotten Fencepost video ->" -> `/campaign/001`), placed in the existing intro block, same "one natural sentence in an existing section" precedent Work Order 008/010 used — so the new page isn't orphaned. The page's pre-existing links (to Product 001, to the wellness article, the buy buttons) are all unchanged.
  - **No changes to `base.html`, `schema.py`, `styles.css`, any other route, or any prior page's content/metadata/structured data** — purely additive.
  - **Testing**: new `tests/test_campaign_pages.py` (13 tests) — 200/404 on known/unknown campaign ids, correct video embed, both required links present, title/meta description correct, canonical tag correct, `WebPage`/`BreadcrumbList` schema present, OG/Twitter tags present, sitemap includes the new URL with no duplicates introduced, and the hub page's new link (plus its pre-existing Work Order 010 product link) both present. Full suite: **311/311 pass** (298 prior + 13 new). Full site crawl via the Flask test client (40 unique internal paths reached from the sitemap plus both pages this work order touched): zero broken links. Manually inspected the rendered HTML of both `/campaign/001` and `/rotten-fencepost` via a local `flask run` instance — confirmed canonical/OG/Twitter tags, both JSON-LD blocks, the video iframe's `src`, and all required links render correctly. The Claude-in-Chrome extension was unavailable this session (same recurring environment limitation noted in earlier entries), so pixel-level visual review in an actual browser (hero image crop/overlay legibility, embed sizing at various viewport widths) was not possible — flagged for a human look before/at deploy verification, same as prior UI work orders.
  - **Not yet committed, pushed, or deployed** — awaiting Kahu Phil's explicit go-ahead, per standing deploy discipline.

### 2026-07-25 (repository maintenance)
- **Discovery Engineering Completion Report** (`DISCOVERY_ENGINEERING_COMPLETION_REPORT.md`, new file). Summarizes Work Orders 005–010, confirms final production state (38-URL sitemap, 298/298 tests, structured data/metadata/canonical/internal-linking coverage as of commit `489fcae`), lists remaining non-code items, and formally declares the Discovery Engineering phase complete. Serves as the baseline for the next phase.
- **Documentation correction, not a new work order**: `GOOGLE_VISIBILITY_READINESS_AUDIT.md`'s roadmap-item-2 status note referenced an erroneous "Work Order 011 review" that was never logged or committed. Replaced with a factual description ("the final roadmap audit conducted after Work Order 010's completion") — no Work Order 011 exists or was created.
- **`.gitignore`**: added `data/subscribers.json` (runtime-generated subscriber list, auto-created empty by `blueprints/downloads.py`/`config.py`'s `SUBSCRIBERS_FILE` — same category as the already-ignored `email_subscribers.json`).

### 2026-07-25
- **Production Deployment Verification** (Work Order 009-DV). Confirmed local commit `3f6cf24237d4dfd22d7803388db6996dfd12e86c` matches `origin/main` and is live on Render. Live `/sitemap.xml`: 200, valid XML, exactly 38 URLs, all 4 Work Order 009 product pages present, zero duplicates/HTTP-URLs/dev-hosts. Live `/robots.txt`: correct directives and sitemap reference. HTTP->HTTPS redirect confirmed clean (301, no loops) on the apex domain and a representative page; `www` subdomain reconfirmed unconfigured (Cloudflare `409`/TLS failure), same pre-existing, non-code-fixable issue already documented in `GOOGLE_VISIBILITY_READINESS_AUDIT.md` Section 9 — not touched. 11 representative pages (including all 4 new product pages) verified 200 with correct canonical tags, no mixed content. 404 handling confirmed correct (real 404 status, no debug traceback); confirmed this codebase has no custom-styled `@app.errorhandler(404)` (Flask default 404 is the actual intended behavior). One-off production crawl (63 URLs from the live sitemap, stdlib-only script, not committed) found zero non-200s, zero redirect loops, zero HTTP links. No corrections were required — production matched local exactly. **Work Order 009 — COMPLETE AND PRODUCTION VERIFIED.**
- **Internal Linking for Newly-Sitemapped Orphan Product Pages** (Work Order 010). Continuation of the still-open item in `GOOGLE_VISIBILITY_READINESS_AUDIT.md` Section 6/engineering-sequence item 6: the 4 product pages Work Order 009 added to the sitemap (`prod_aloha_wellness`, `prod_kingdom_booklet1`, `prod_kingdom_booklet2`, `prod_nahenahe_cd`) had zero inbound links from any public page (confirmed by grep — each was reachable only via direct checkout links, external stores, or the sitemap itself, never via its own `/product/<id>` page). Added exactly one natural, contextual link to each, from a page already discussing that same product, in `content.py` only:
  1. `kingdom_wealth` page's existing "Start Your Kingdom Wealth Journey" closing link list -> new line linking to `/product/prod_kingdom_booklet2` ("Kingdom Wealth" free booklet).
  2. `call_to_repentance` page's "The Kingdom Series" section -> new sentence linking to `/product/prod_kingdom_booklet1` ("The Kingdom Is Here" free booklet).
  3. `aloha_wellness` page's existing closing availability line -> extended with a link to `/product/prod_aloha_wellness`.
  4. `nahenahe_voice` page's "Listen Now" section -> new sentence linking to `/product/prod_nahenahe_cd`.
  - **Deliberately left untouched**, per already-locked decisions: `/product/rotten_fencepost_field_guide` (Work Order 009 intentionally excluded it from the sitemap as a near-duplicate of `/rotten-fencepost`), the `/aloha-wellness` hyphen funnel and `/myron-golden` (Work Order 008 deliberately excluded these ad-landing pages from new internal links), and `partner_tier1`-`4` (still an open editorial call per the original audit, not resolved here).
  - Also corrected two stale status markers in `GOOGLE_VISIBILITY_READINESS_AUDIT.md`'s engineering-sequence table: item 4 (`noindex` on transactional pages) was already done in Work Order 007 but still read "Not started"; item 6 updated to record this work order's addition.
  - **Testing**: no new tests added (pure content/data, same precedent as Work Order 005's meta description work). Full suite: **298/298 pass**, unchanged. Verified programmatically via the Flask test client: all 4 source pages render 200 and contain the new link markup; all 4 target product pages resolve 200.
  - **Not yet committed, pushed, or deployed** — awaiting Kahu Phil's explicit go-ahead, per standing deploy discipline.

### 2026-07-24
- **Search Engine Readiness & Indexing Preparation** (Work Order 009). Audit-first work order (structured-data, metadata, and internal-link changes explicitly prohibited) covering technical indexing health, sitemap completeness, robots.txt, and Search Console readiness.
  - **Genuine defect found and fixed — sitemap gap.** 4 active, publicly reachable, for-sale product pages (`/product/prod_aloha_wellness`, `/product/prod_kingdom_booklet1`, `/product/prod_kingdom_booklet2`, `/product/prod_nahenahe_cd`) were missing from `sitemap.xml` despite being in the same category (real catalog item, `Product`/`Offer` JSON-LD already present since Work Order 007, no `noindex`, self-referencing canonical already present via `base.html`) as `prod_find_the_cause_not_the_symptoms`, which was already included. Added all 4 to `blueprints/pages.py`'s sitemap route.
  - **Deliberately left out of the sitemap, documented in a new code comment on the `sitemap()` route**: `/product/rotten_fencepost_field_guide` (would be near-duplicate content — `/rotten-fencepost` is already this exact product's canonical, indexed sales page) and `/product/partner_tier1`-`partner_tier4` (donation tiers; `/partner` is the canonical entry point, matching the existing, still-open call in `GOOGLE_VISIBILITY_READINESS_AUDIT.md` Section 6 that these are "likely intentional, not a clear-cut gap").
  - **Genuine defect found, NOT fixable in this codebase** — `www.keaupuniakeakua.faith`: DNS resolves (Cloudflare-proxied, pointing at the same Render origin as the apex domain), but the hostname has no working TLS configuration or redirect rule. HTTPS requests fail with a TLS handshake failure (alert 40) before any HTTP response is even sent; plain HTTP requests return a bare `409 Conflict` from Cloudflare (error code 1001, DNS/origin resolution failure at the edge) instead of a redirect to the canonical apex domain. Confirmed via `openssl s_client` and raw HTTP requests, not a client-side artifact. Searched `app.py` for any host-based logic — none exists; this is entirely a Cloudflare/Render dashboard DNS and custom-domain configuration issue, outside the Flask application. Reported as a manual action item (same category as the existing Search Console/Bing manual tasks) — either remove the `www` DNS record entirely (clean non-resolution) or properly configure it as a custom domain alias with a 301 redirect to the apex domain. Not attempted here; requires Cloudflare/Render account access this session does not have.
  - **Everything else audited, confirmed correct, left unchanged**: HTTP→HTTPS redirect (301, confirmed live), robots.txt (`Allow: /`, `Disallow: /kahu`, `Disallow: /admin`, correct `Sitemap:` line), trailing-slash behavior (Flask's standard no-slash-match 404 on trailing-slash variants — a non-issue since zero internal links anywhere in the codebase use a trailing slash, confirmed by search), case sensitivity (mismatched-case paths correctly 404, never linked internally), zero mixed-content/hardcoded `http://` links anywhere in templates or `pages.py`, `noindex` present on all 3 checked transactional pages and absent from all 23 checked public/indexable pages (including the 4 newly-sitemapped product pages), all JSON-LD blocks sitewide still valid JSON, 404 handling for nonexistent paths returns real 404s (no soft-404s), viewport meta tag present on every public template (mobile-first indexing baseline). `partner_tier1`-`4` product pages have no `noindex` and remain out of the sitemap — not private, just orphaned/unlinked; left as the same open, non-clear-cut item the prior audit flagged, not resolved here (would require an editorial call outside "genuine technical defect" scope).
  - **No structured data, metadata, layout, or internal links were modified** — the work order explicitly prohibited this; the pre-existing absence of JSON-LD on the 3 standalone funnel/study templates (`aloha_wellness_funnel.html`, `kingdom_study.html`, `myron_golden_funnel.html`) was confirmed during the audit but not touched, consistent with that prohibition.
  - **Testing**: new `tests/test_indexing_readiness.py` (28 tests) — sitemap completeness/exclusions/no-duplicates/all-URLs-resolve-200, robots.txt directives, noindex presence/absence correctness, real-404 handling. Full suite: **298/298 pass** (270 prior + 28 new). Full local crawl (32 unique internal paths from 15 entry points): zero broken links. Checkout/PayPal/Stripe/download-token regression subset (59 tests) re-run explicitly: all pass.
- **Canonical Tags & Internal Link Architecture** (Work Order 008). Two-part work order: correct canonical tag coverage sitewide, and add a small number of genuinely natural internal links between related content.
  - **Part 1 — Canonical tags.** Investigation (`grep -rln 'rel="canonical"' templates/`) found exactly one canonical implementation sitewide: a single line in `templates/base.html` (`<link rel="canonical" href="https://keaupuniakeakua.faith{{ request.path }}" />`), inherited automatically by every template that extends `base.html`, with zero duplicates anywhere. The gap was the site's 6 standalone templates (don't extend `base.html`), of which 3 are public, non-transactional pages that had no canonical tag at all: `aloha_wellness_funnel.html` (`/aloha-wellness`), `kingdom_study.html` (`/kingdom-study`), `myron_golden_funnel.html` (`/myron-golden`). Added one absolute-HTTPS, self-referencing `<link rel="canonical">` to each, matching the exact pattern `base.html` already uses. The other 3 standalone templates (`partner_success.html`, `payment_success.html`, and the dead/unrouted `myron_golden.html`) were correctly left untouched — the first two are transactional confirmation pages explicitly excluded by this work order's own scope, and already carry `noindex` from Work Order 007.
  - **Existing exception, documented rather than changed**: `checkout.html`, `thank_you.html`, and `rotten_fencepost_success.html` extend `base.html` and therefore already inherit a self-referencing canonical — this predates this work order. Left in place: it's harmless alongside their Work Order 007 `noindex, nofollow` tag (a `noindex` page can still validly declare its own canonical), and building new suppression logic to remove it would be new engineering for no benefit, which this work order did not ask for.
  - Admin routes/templates were confirmed out of scope, same as prior work orders — already blocked by `robots.txt` and login-gated.
  - **Part 2 — Internal linking.** Investigated the site's existing link graph (`_SEO_PAGES` in `blueprints/pages.py`, `DEFAULT_PAGES` in `content.py`) before adding anything, per the work order's "do not force links where unnatural" constraint. Added exactly 4 links, all between pages that already share the same subject matter:
    1. `/wellness/the-rotten-fencepost-principle` (article explaining the concept) → `/rotten-fencepost` (the Field Guide product that applies it) — added as a new sentence in the article's existing closing link list.
    2. `/rotten-fencepost` → `/wellness/the-rotten-fencepost-principle` — added to the page's existing "More From the Rotten Fencepost Series" section, alongside its existing cross-sell to `prod_find_the_cause_not_the_symptoms`.
    3. `/kingdom/understanding-scripture-through-original-words` (article on why original-language study matters) → `/kingdom-study` (the paid 73-word-study product) — added as a new sentence after the article's existing link to the free Scripture Language tool.
    4. `/scripture-tools/hebrew-greek-meaning-tool` (the free tool's own page) → `/kingdom-study` — added as a natural upsell sentence in the page's existing "Begin with the Tool" closing section.
  - **Deliberately not added**, to honor "quality over quantity": a link to `/aloha-wellness` (an ad-landing funnel page, not organic content), a forced Rotten Fencepost mention in `/ecosystem`'s prose (no natural sentence for it), and a hardcoded product-to-product cross-link inside the generic `product_page.html` template (would apply to products where it doesn't fit). No paragraphs were rewritten, no keyword stuffing, no footer link farm, no visual/design changes — every new link is a single new sentence using natural anchor text, placed in an existing content or link section.
  - **Testing**: new `tests/test_canonical_and_internal_links.py` (24 tests) — canonical presence/correctness/absolute-HTTPS/no-duplicates on both `base.html`-inherited and newly-corrected standalone pages, and presence + 200-resolution of all 4 new internal links. Full suite: **270/270 pass** (246 prior + 24 new). Full local crawl of all 34 sitemap URLs: every page has exactly one canonical tag, absolute HTTPS, matching its own path — zero duplicates, zero mismatches, zero missing. Checkout/PayPal/Stripe/download-token regression subset (58 tests) re-run explicitly: all pass, unaffected (no payment, download, or token code was touched).
- **Structured Data / Schema.org Implementation** (Work Order 007). Previously the site had exactly two JSON-LD blocks (Person + Organization), unlinked, present on every `base.html` page. Added `WebSite`, `WebPage`/`CollectionPage`, `Article`, `Product`, and `BreadcrumbList` schema, sourced entirely from data that already existed on the site — no invented dates, ratings, reviews, prices, addresses, or identifiers.
  - **New `schema.py`**: centralized builder functions (`build_webpage_schema`, `build_article_schema`, `build_product_schema`, `build_breadcrumb_schema`, `build_collection_itemlist_schema`) that return plain Python dicts, rendered via Jinja's `tojson` filter rather than hand-assembled JSON text — guarantees syntactically valid JSON regardless of which optional fields are present, avoids the comma-matching fragility of conditional Jinja blocks inside a JSON literal.
  - **`templates/base.html`**: the old two separate Person/Organization `<script>` tags became one `@graph` with stable `@id` values (`#organization`, `#person`, `#website`) so page-specific schema can reference them instead of repeating their data. Added `logo` (the site's actual logo file) and `sameAs` (the site's real, already-linked YouTube channel — the only genuinely established social profile found; no others invented) to Organization. Added a new `WebSite` entity. **No `SearchAction`** — the site has no working site-search feature, and the work order explicitly said not to add one merely for appearance. Page-specific schema renders from three template variables (`page_schema`, `breadcrumb_schema`, `product_schema`) that routes now pass in.
  - **Schema map by page category:**
    - Home + 7 primary content pages (`page.html` via `render_page()`) → `WebPage`
    - `/ecosystem` → `CollectionPage` (genuinely functions as a hub to the site's 3 real content pillars, using the same links its own "Begin Where You Are" section already has)
    - `/products` → `CollectionPage` with a lightweight `ItemList` (name/url only — explicitly not `Product` schema on the listing page, per instruction)
    - `/product/<id>` (non-partnership) → `Product` + `Offer` (real catalog price/currency/availability) + `BreadcrumbList` (Home → Products → Product)
    - `/product/partner_tier1-4` → **no** `Product` schema — donation/membership tiers aren't goods for sale, so `Product` would misdescribe them; they still render normally, just without commerce schema
    - `/rotten-fencepost` → `Product` schema for the real `rotten_fencepost_field_guide` catalog entry (this bespoke template genuinely is that product's sales page, even though its buy buttons skip the generic `/product/<id>` route) — image uses the page's own hero photo, not another product's cover, after catching and fixing that mistake mid-implementation
    - 18 SEO article pages (`seo_subpage()`) → `Article` + `BreadcrumbList` (Home → Article, 2 levels only — no `/wellness`, `/kingdom`, etc. index page exists to link a middle "category" level to, and inventing one just for the breadcrumb would be exactly the fabrication the work order prohibited)
    - `/partner` → `WebPage`
    - Transactional/private pages (`/checkout/<id>`, `/thank-you`, `/rotten-fencepost/success`, PayPal/Stripe/partnership success pages) → **no promotional schema at all**, plus a new `<meta name="robots" content="noindex, nofollow">` (see below)
  - **`datePublished`/`dateModified` deliberately omitted everywhere** — no reliable publish/modification date exists anywhere in this site's data model (confirmed by search before implementing), and the work order explicitly prohibited inventing them.
  - **Pages that should not be indexed**: none of the site's transactional pages had `noindex` before this work order (confirmed by search). Added `<meta name="robots" content="noindex, nofollow">` to `checkout.html`, `thank_you.html`, `rotten_fencepost_success.html`, `partner_success.html`, and `payment_success.html` — a minimal, explicitly-authorized correction ("essential to prevent private transactional content from being indexed"), not a broader indexing-rules change. Admin routes (`/kahu`, `/admin/*`) were left as-is — already blocked by `robots.txt` `Disallow` and require login, so no additional correction was needed there. `/download/product/<id>/<token>` is a raw file response with no HTML `<head>` to add a meta tag to, and its single-use signed token means the URL isn't stable/discoverable anyway — reported, not changed.
  - **Testing**: added `tests/test_structured_data.py` (66 tests) covering JSON validity, correct schema type per page category, product price accuracy against the live catalog, consistent `@id` values, absolute HTTPS URLs, absence of fabricated fields (ratings/reviews/SKU/GTIN), and noindex + schema-exclusion on transactional pages. Full suite: **246/246 pass** (180 prior + 66 new). Full crawl (56 links this pass, since product-detail links were included): zero broken links. Every image URL referenced in any schema block confirmed to load (200). Checkout/PayPal/Stripe/download-token behavior and all visible page content confirmed unchanged.
- **Open Graph & Social Sharing Metadata Implementation** (Work Order 006). Implemented all 10 required Open Graph/Twitter Card fields (`og:title`, `og:description`, `og:type`, `og:url`, `og:image`, `og:site_name`, `twitter:card`, `twitter:title`, `twitter:description`, `twitter:image`) across every public page — previously only `product_page.html` had any of this.
  - **`templates/base.html`**: added a shared OG/Twitter block using `self.title()`/`self.meta_description()` to mirror each page's already-declared `<title>`/`<meta description>` automatically, so no page has to restate its own metadata a second time (directly satisfies "no duplicate template logic"). Twitter tags in turn default to whatever `og_title`/`og_description`/`og_image` resolved to. Default `og:image` falls back to the site logo (absolute URL via `request.url_root`); `og:type` defaults to `website`; `twitter:card` defaults to `summary_large_image`. A page only needs to override `{% block og_image %}` (etc.) when it has something more specific — e.g. its own hero photo.
  - **`templates/page.html`**: added `{% block og_image %}` using `page.hero_image` — covers all 28 pages rendered through this one template (home + the 26 corrected pages + `/ecosystem`) with a single line.
  - **`templates/partner.html`, `rotten_fencepost.html`, `products.html`**: added `{% block og_image %}` pointing at each page's own existing hero photo.
  - **`templates/product_page.html`**: migrated its pre-existing hand-written OG block (added in Work Order 002) into the new shared system — `og_type`/`og_title`/`og_description`/`og_image` overrides instead of a duplicate `extra_head` block. Its old conditional `og:image` (omitted entirely when a product had no `cover_image`) now falls back to the site logo instead of omitting the tag, so every product page guarantees a preview image.
  - **`templates/kingdom_study.html`, `myron_golden_funnel.html`, `aloha_wellness_funnel.html`** (standalone templates, don't extend `base.html`): added the same 10 tags directly, reusing each page's existing title/description. `aloha_wellness_funnel.html` had no `<meta name="description">` at all before this — added one (necessary to have any `og:description`/`twitter:description` value at all; kept minimal and description-only, no other content touched).
  - **Incidental bug fix required to make this work correctly**: `/thank-you` (`blueprints/downloads.py`) was the only route rendering a `base.html`-extending template without passing `logo_path`/`logo_height`/`footer_text` at all — meaning its nav logo was already silently broken (`src=""`) before this work order, and would have produced a broken `og:image` URL under the new mechanism. Added the three missing kwargs, restoring the same pattern every other route already uses.
  - **Testing**: 180/180 pass. Verified programmatically across all 36 public pages (not sampled): all 10 fields present exactly once on every page (zero missing, zero duplicated), every `og:image` is a real absolute URL, and images are correctly page-specific (spot-checked 9 pages, each showing a different, correct image). Full 35-page/49-link crawl: zero broken links. Checkout/PayPal/Stripe/download-token behavior confirmed unchanged.
- **Unique Metadata Implementation** (Work Order 005 "Unique Metadata Implementation" — note: reuses the "005" label already used earlier today for "Sitemap Completion"; distinguished here by title, not number). Addressed the audit's Section 4 finding that 8 of 9 primary content pages shared one identical, generic fallback meta description (`templates/page.html:4`).
  - **`content.py`**: added a unique `meta_description` to all 7 affected `page.html`-driven pages (`kingdom_wealth`, `aloha_wellness`, `call_to_repentance`, `pastor_planners`, `nahenahe_voice`, `free_booklets`, `kingdom_keys`) — the 8th, `partner`, is dead data in this file (the live `/partner` route uses `partner.html` directly, confirmed in the Work Order 004-B investigation) so it was addressed via its real template instead. Also added a new `seo_title` field to the same 7 pages.
  - **`templates/page.html`**: `{% block title %}` now reads `page.get("seo_title", page.title)` instead of always `page.title`. This was the key design decision: `page.title` is *also* what the hero's visible `<h1>` renders (`<h1>{{ page.title }}</h1>`, unchanged) — introducing a separate `seo_title` field means the `<title>` tag (browser tab / search result) could be improved without touching a single word of visible on-page content, matching the work order's "do not redesign content" constraint exactly. Verified by direct comparison: every one of the 7 pages' rendered `<h1>` is byte-for-byte identical to before this change, while its `<title>` is new and unique.
  - **`templates/partner.html`**, **`templates/rotten_fencepost.html`**: added `{% block meta_description %}` (neither had one at all before — falling back to the same generic sitewide default). Titles on both were already unique, so left untouched.
  - All 7 new titles are 44–53 characters (well under the ~60-char guideline); all 9 new/changed descriptions are 132–155 characters (within the ~155–160 guideline). Verified programmatically unique against each other, against `/` and `/ecosystem`'s existing descriptions, and against all 18 `_SEO_PAGES` descriptions — zero collisions anywhere on the site.
  - **Testing**: 180/180 tests pass (no new tests added — this is pure content/data, already covered by existing page-render and crawl tests). Full 35-page/49-link crawl: zero broken links.
- **Full-Page Background Restoration** (Work Order 004-D). Live visual review of 004-C found an aesthetic regression: on the 26 corrected pages, the hero image now visibly ends after one screen and the body/footer below feels flat and disconnected. **Investigation finding:** the old "continuous ambiance" was never a deliberate background treatment anywhere in the code — it was an accidental side effect of the overlap bug itself. `.container`'s old `position: absolute; height: 100vh; overflow-y: auto` meant most of a visitor's actual article-reading happened scrolling *inside* that fixed box, during which the hero image (a separate, opaque element occupying that same screen region) stayed visible behind `.content-card`'s semi-transparent panel the whole time, because the hero was never really being scrolled away. Fixing the overlap and losing that accidental ambiance were never separable.
  - **Fix** (`templates/base.html`, `templates/page.html`, `templates/partials/styles.css`): added an empty-by-default `{% block body_attrs %}` to `<body>` in `base.html`; `page.html` fills it with `style="--page-hero-bg: url('{{ page.hero_image }}');"` — the same image URL its hero already uses. `body`'s `background-image` gained a third layer, `var(--page-hero-bg, none)` (`none` by default, so every non-`page.html` template is unaffected), with `background-size: cover; background-position: center center; background-attachment: fixed` — a standard parallax pattern, matching the hero's own `background-position: center` for the smoothest handoff. `fixed` attachment falls back to `scroll` at `≤768px` (unreliable on mobile browsers). This is a pure CSS-painting mechanism with no relationship to element position/document flow, so it cannot reintroduce the overlap the 170 existing regression tests guard against.
  - Deliberately did **not** add a fallback `background-color` to `body` — 7 templates extend `base.html` without defining their own `body{}` rule (`rotten_fencepost.html`, `partner.html`, `product_page.html`, `checkout.html`, `products.html`, `thank_you.html`, `rotten_fencepost_success.html`) and currently rely on the shared plain background; a solid color would have visibly changed the tint behind several of their own semi-transparent panels, which this work order explicitly said not to touch. Only the new image layer was added.
  - **Homepage**: same underlying discontinuity exists after its own hero (`.home-content-card`, further down the page, sits on the same plain background), softened but not eliminated by Correction 003-A's colorful promo bands in between. Same fix applied (home's `hero_image`, `molokai_coast.jpg`) with zero changes to `.home-hero`/`.home-container`/`.home-content-card`, hero content, both CTA buttons, or section order — confirmed intact.
  - Added 10 tests to `tests/test_shared_layout.py`: variable-matches-hero-image checks on 4 representative pages, a regression guard on `body`'s CSS rule (must contain the `var(--page-hero-bg` layer, must not contain `background-color`), and confirmation that 5 independent templates' `<body>` tags carry no attributes at all. Full suite: **180/180 pass** (170 prior + 10 new). Full 35-page/49-link crawl: zero broken links. Commerce/checkout/PayPal/download-token behavior unaffected (verified identical to prior work orders).
- **Sitewide Shared-Layout Correction** (Work Orders 004-B investigation + 004-C implementation). 004-B found that the exact hero/body overlap defect fixed on the homepage in Correction 003-A (`.container`'s `position: absolute; top: 0; height: 100vh`, layering page content on top of the hero instead of after it) was still present, unmodified, on **26 other pages** — every page rendered through `page.html` except home (the 7 primary content pages, `/ecosystem`, and all 18 wellness/kingdom/wealth/scripture-tools article pages). Desktop-only; an existing mobile override already made `.container` safe below 768px, likely why it went unnoticed. Full investigation and page-by-page inventory in `SITE_LAYOUT_AUDIT.md`.
  - **004-C fix** (`templates/partials/styles.css`): removed `.container`'s absolute positioning and `.content-card`'s `margin-top: 25vh` peek-through offset, replacing both with the same plain normal-flow layout `.home-container`/`.home-content-card` already used successfully. Deviated from the original investigation's suggested approach (duplicate the homepage's classes under new names) once it was confirmed *every* use of `.container`/`.content-card` needed the fix — editing them directly was simpler and covers all 26 pages with one change instead of 26 template rewrites. `page.html`'s existing home/non-home conditional needed no changes at all, since both branches are now safe. `.home-hero`/`.home-container`/`.home-content-card` and all 11 independently-safe templates (`rotten_fencepost.html`, `partner.html`, `product_page.html`, `checkout.html`, `products.html`, `thank_you.html`, and the 5 standalone funnel/success templates) were left untouched, confirmed via diff.
  - Added `tests/test_shared_layout.py` (59 tests): every one of the 26 affected pages returns 200 and renders via the shared classes; a direct regression guard asserting `.container` never contains `position: absolute` again; confirms the homepage still uses its own `.home-container`/`.home-content-card`; spot-checks the independently-safe templates. Full suite: 170/170 pass (111 existing + 59 new). Full 35-page/49-link crawl: zero broken links. Verified representative pages (`/kingdom_wealth`, `/ecosystem`, and `/kingdom/understanding-scripture-through-original-words` — the longest article page on the site, chosen to stress-test long-content flow) render in correct hero→container→article→footer document order with no absolute positioning in their inlined CSS. Product, checkout, PayPal/Stripe, and download-token behavior all unaffected.
- **Sitemap Completion** (Work Order 005) — `blueprints/pages.py`'s `sitemap()` route. Scoped strictly to the sitemap; no metadata, structured data, or other visibility work touched, per instruction.
  - **Added 7 pages** identified as missing in the Work Order 004 audit: `/ecosystem` (0.9, weekly), `/partner` (0.8, monthly), `/rotten-fencepost` (0.9, weekly — now the homepage's primary CTA), `/aloha-wellness` (0.8, weekly — the hyphenated funnel page, distinct from `/aloha_wellness`), and the 3 missing `/scripture-tools/*` pages (`hebrew-greek-meaning-tool` at weekly since it's the flagship page referenced by name from `/ecosystem`; `translation-gap-in-scripture` and `original-language-meaning` at monthly, matching the site's existing pattern for less-actively-cross-linked reference articles). Sitemap is now 34 URLs (was 27).
  - **`<lastmod>` removed entirely**, not fixed to a "better" value. Investigated whether an accurate per-page value was achievable: this app has no CMS or timestamp layer anywhere in its data model (page content lives in hardcoded Python dicts in `content.py` and `_SEO_PAGES` in `blueprints/pages.py`, with no "last modified" field on any page). The only way to get a *real* date would be either adding a genuine content-timestamp system (well beyond this work order's scope) or shelling out to `git log` per file at request time (fragile — depends on `.git` being present and readable in the Render deploy, adds a subprocess call to a hot request path, and still only gives file-level granularity, not per-page, since multiple pages share one file). Both fail the work order's own "without introducing unnecessary complexity" bar. The previous behavior (`datetime.now()` stamped on every URL, every request) wasn't accurate either — it just always said "today," which is arguably a mild form of the "invented modification date" the work order explicitly prohibited. `<lastmod>` is optional per the sitemap protocol, so removing it entirely was the only honest option left; also removed the now-unused `from datetime import datetime` import.
  - **Validation**: sitemap re-confirmed well-formed XML, 34 URLs, **zero duplicates**, every URL resolves 200 (both locally and live), every `<loc>` matches that page's own `<link rel="canonical">` tag (the 3 pages with no canonical tag at all — `/myron-golden`, `/aloha-wellness`, `/kingdom-study` — are a pre-existing, already-documented metadata gap from the Work Order 004 audit, correctly left untouched here). `robots.txt` still correctly references the sitemap. Full 111-test suite passes. Full 35-page/49-link crawl: zero regressions.
  - Also updated `GOOGLE_VISIBILITY_READINESS_AUDIT.md` to mark the sitemap-omissions and `<lastmod>` recommendations complete/resolved, with a status column added to the recommendation table.
- **Google Visibility Readiness Audit** (Work Order 004) — understanding-only, no code changed. Full findings in `GOOGLE_VISIBILITY_READINESS_AUDIT.md`. Files inspected: `templates/base.html`, `templates/page.html`, all 5 standalone templates (`aloha_wellness_funnel.html`, `kingdom_study.html`, `myron_golden_funnel.html`, `partner_success.html`, `payment_success.html`), `blueprints/pages.py` (sitemap/robots/`_SEO_PAGES`), `content.py` (`DEFAULT_PAGES` meta_description coverage), `products.py`. Major findings: (1) Search Console + Bing verification already present via meta tags, no code needed, but GSC/Bing property-claim status can't be confirmed from code — manual check required. (2) `sitemap.xml` is well-formed and every listed URL resolves, but omits 6 real pages (`/ecosystem`, `/partner`, `/rotten-fencepost`, 3 of 6 `/scripture-tools/*` pages) and its `<lastmod>` is always "today" regardless of actual changes. (3) `robots.txt` correctly allows all real content and blocks only `/kahu`/`/admin` — nothing accidentally blocked. (4) 8 of 9 primary content pages share one identical generic meta description (`templates/page.html:4`'s fallback); Open Graph/Twitter Card tags exist only on `product_page.html`, nowhere else sitewide. (5) Structured data is limited to `Person` + `Organization` JSON-LD (both valid); no `Product`, `Article`, `WebSite`, or `BreadcrumbList` schema anywhere. (6) Found genuinely orphaned pages with zero inbound internal links from any public page: `/aloha-wellness` (the hyphenated funnel — distinct from `/aloha_wellness`) and `/product/rotten_fencepost_field_guide`, plus the 4 partner-tier product pages (likely intentional, since `/partner` has its own dedicated checkout UI). Full prioritized 10-item engineering sequence with effort estimates and code-only vs. Google-account-access flags is in the audit doc's closing table. **No implementation begun — awaiting approval for Work Order 005.**
- **Repaired the 5 Critical findings from `WEBSITE_VISIBILITY_AUDIT_001.md`** (Work Order 002). Approved by Kahu Phil, committed (`31827a4`), pushed, and deployed to Render. **Live-verified via HTTP against `keaupuniakeakua.faith`**: all 12 nav destinations return 200 and the new grouped/dropdown markup is live; hero H1s render with real content on both short and long titles; product/checkout pages carry site nav, footer, and working back-links (no purchase was attempted); all 14 repaired internal links resolve and a full 35-page/49-link live crawl found zero 404s; `/rotten-fencepost/success` renders with its new dark card styling and both return links work. See `WEBSITE_VISIBILITY_AUDIT_001.md` for full before/after detail on each item. Pixel-level visual checks (exact nav wrapping/spacing across viewport widths, dropdown open/close feel, mobile presentation) still need a human look in a real browser — the Claude-in-Chrome extension was unavailable all session.
  - **Nav overflow** (`templates/base.html`, `templates/partials/styles.css`): grouped the 6 least-critical of 12 nav items behind a "More" dropdown, reusing `.nav-dropdown`/`.dropdown-menu` CSS and click-outside JS that already existed in the codebase but was never wired to any markup. Added a `toggleDropdown()` click handler. Added `flex-wrap` to `.nav-menu` as a safety net. Moved the hamburger breakpoint from 768px to 1100px (the horizontal bar can't reliably fit even the grouped nav at 1024px next to the existing 180px-tall logo) and trimmed nav link font-size from 1.3rem to 1.1rem for extra margin at 1101px+. All 12 original destinations remain reachable.
  - **Hidden page titles** (`templates/partials/styles.css`): removed `.hero h1 { display: none; }`, replaced with visible, responsive (`clamp()`-sized), text-shadowed styling. Affects all ~28 `page.html`-driven pages. Verified all `page.title` values in `website_content.json` and the `_SEO_PAGES` dict render cleanly (no odd whitespace).
  - **Off-brand product/checkout pages** (`templates/product_page.html`, `templates/checkout.html`): both now `{% extends "base.html" %}` instead of being standalone documents, so they carry the site nav, footer, and branding. Added an `extra_head` block to `base.html` so `product_page.html` can still inject its OG/Twitter meta tags. Renamed each page's own `.container` class to avoid colliding with the shared `.container` rule from `page.html`'s hero layout. Added explicit "Back to All Products" / "Return to Homepage" links (product page) and "Back to product page" / "Return to Homepage" links (checkout). All existing Stripe form actions, the PayPal SDK script/JS, and the `#paypal-button-container` id were carried over unchanged. Incidentally fixed raw (non-2-decimal) price display on the product page (`$23.5` → `$23.50`) while rewriting that block.
  - **Broken internal links** (`blueprints/pages.py`): repointed every instance of `/support` → `/partner`, bare `/scripture-tools` → `/scripture-tools/hebrew-greek-meaning-tool`, bare `/kingdom` → `/call_to_repentance`, and `/wellness/what-actually-works-instead-of-dieting` → `/wellness/lose-weight-without-dieting`. No redirects added: these were never valid routes (not renamed/moved pages), so nothing could have indexed or bookmarked them as working URLs.
  - **White-on-white success page** (`templates/rotten_fencepost_success.html`): wrapped content in a new `.success-card` block (explicit dark background/text color, kept separate from `.content-card` to avoid a specificity collision with `.content-card a` that would have recolored the buttons). Added "Back to Rotten Fencepost" and "Return to Ke Aupuni O Ke Akua" links. Confirmed via repo-wide search that no payment success/redirect flow currently links to this route — it appears to be orphaned from an earlier version of the funnel — but it now renders correctly if reached directly.
  - **Testing:** full existing suite (`tests/`, 111 tests) passes. Manually exercised every changed/affected route via the Flask test client (including checkout with PayPal/Stripe credentials monkeypatched present), crawled all internal links across all 35 public pages (46 non-checkout links, zero 404s), and confirmed no protected file paths leak from the rewritten product/checkout pages. Nav width fix is code-reviewed and measured against the actual 180px logo height and container max-width, but **not yet visually verified in a live browser** at the requested viewport widths (1920/1440/1366/1024/768/390) — the Claude-in-Chrome extension was unavailable both this session and the prior audit session. Flagged for live human review before/at deploy verification.
- **Added Product 001: "Find the Cause, Not the Symptoms"** (`prod_find_the_cause_not_the_symptoms` in `digital_products.json`, $9.99, category `ebook`). Uses the existing generic `/product/<id>` + `product_page.html` flow — no bespoke funnel page. `product_page.html` gained optional `subtitle`/`author`/`publisher`/`series`/`seo_title`/`meta_description` fields (backward compatible, unused fields are simply ignored by older entries).
- **New public `/products` listing page** (`blueprints/products.py` + `templates/products.html`) — lists all `active` non-`partnership` catalog products as cards. Added to nav in `base.html` and to `sitemap.xml`.
- Approved cover (`static/covers/find_the_cause_not_the_symptoms_cover.jpg`, full KDP wraparound print spread) and finalized PDF (`digital_products/find_the_cause_not_the_symptoms.pdf`) received from Kahu Phil and installed. Generated `find_the_cause_not_the_symptoms_cover_web.jpg` — a front-cover-only crop — for on-site display (`cover_image`); the untouched wraparound file is kept as `print_cover_image` for reference and is not itself displayed anywhere on the site. Product 001 is now `"active": true`.
- Added a homepage promo band (`content.py`, `home` page `body_md`) linking to the new product page.
- Added `og:title`/`og:description`/`og:image`/`twitter:card` meta tags to `product_page.html` (none existed anywhere on the site before) so product links share correctly.
- **Bugfix:** `serve_cover()` in `blueprints/downloads.py` (a leftover route from before the blueprint refactor) resolved `/static/covers/<filename>` against the repo root instead of `static/covers/`, so it shadowed Flask's default static handler and 404'd on every file in that folder — including the pre-existing `cover1.jpg`/`cover2.jpg`/`cover3.jpg`, not just the new cover. Fixed the path join; confirmed all four now serve correctly.
- All changes tested locally via `flask run` (nav, homepage band, `/products`, `/product/prod_find_the_cause_not_the_symptoms`, cover image, `/download/product/...`, checkout route, sitemap.xml). Not yet committed, pushed, or deployed — awaiting Kahu Phil's explicit go-ahead.

### 2026-03-03
- **Dynamic YouTube embed on `/aloha-wellness` funnel** — `funnel_youtube_url` stored in `website_content.json` (top-level key). Admin can paste any YouTube URL format into the `/kahu` panel "Weekly YouTube Video" field to update the embed without code changes. `_youtube_to_embed()` in `admin.py` converts share/watch/embed URLs to embed format.
- **Fixed `edit_page` save** — now preserves all top-level JSON keys (was previously stripping unknown keys like `funnel_youtube_url`).

### 2026-02-26
- **Removed all hardcoded secrets from `config.py`** — admin password, PayPal credentials, and SMTP credentials now require environment variables with no fallback defaults. Stripe vars kept optional with empty defaults since Stripe is not yet active.
- **Audited `aloha_wellness_funnel.html`** — confirmed all CTA buttons already route to internal `/checkout/prod_aloha_wellness` (no external Amazon/Gumroad links present).
